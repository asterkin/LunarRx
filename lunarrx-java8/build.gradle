version = '0.1.0'

dependencies {
  compile     'com.netflix.rxjava:rxjava-core:0.+'
  testCompile 'junit:junit:4.+'
  compile     project(':lunarrx-core')
}

sourceSets {
  test {
      java {
          srcDir 'src/test/java'
          srcDir 'src/examples/java'
      }
  }
}

